<?xml version="1.0" encoding="UTF-8"?>
<!--
  Default CycloneDDS profile for the tutorial — auto-detects the
  network interface based on the default route, so every demo
  (toy sim, real-G1 sim, lessons) works on any host without manual
  interface tweaking.

  When you connect to a REAL Unitree G1 over wired ethernet on the
  192.168.123.0/24 segment, swap to install/cyclonedds_g1.xml which
  pins the interface explicitly. See docs/unitree_g1.md.
-->
<CycloneDDS xmlns="https://cdds.io/config"
            xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
            xsi:schemaLocation="https://cdds.io/config https://raw.githubusercontent.com/eclipse-cyclonedds/cyclonedds/master/etc/cyclonedds.xsd">
  <Domain Id="any">
    <General>
      <Interfaces>
        <NetworkInterface autodetermine="true" priority="default" multicast="default" />
      </Interfaces>
      <AllowMulticast>true</AllowMulticast>
      <MaxMessageSize>65500B</MaxMessageSize>
    </General>
    <Internal>
      <Watermarks>
        <WhcHigh>500kB</WhcHigh>
      </Watermarks>
    </Internal>
  </Domain>
</CycloneDDS>
